{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: compile - packages/vscode",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/packages/vscode"
      },
      "problemMatcher": "$tsc",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
